{% extends 'base.html.twig' %}
<div class="container">

	{% block body %}
		<div class="d-flex">
			<a href="{{ path('app_user_index') }}">/icon/arrow-rotate-left-solid.svg</a>
			<h1>Modifier l'utilisateur</h1>
		</div>
		{{ include('user/_form.html.twig', {'button_label': 'Update'}) }}


		{{ include('user/_delete_form.html.twig') }}
	</div>
{% endblock %}
