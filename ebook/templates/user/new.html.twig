{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		<div class="d-flex">
			<button class="ms-3 btn btn-lg text-warning"><a href="{{ path('app_user_index') }}"><i class="bi bi-arrow-counterclockwise"></i></a></button>
			<h1 class="ms-3">Nouveau</h1>
		</div>

		{{ include('user/_form.html.twig' ) }}
	</div>
{% endblock %}
