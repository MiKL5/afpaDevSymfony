{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		<h1>Réinitialiser le mot de passe</h1>

		{{ form_start(resetForm) }}
		{{ form_row(resetForm.plainPassword) }}
		<button class="btn btn-sm btn-success">Réinitialiser</button>
		{{ form_end(resetForm) }}
	</div>
{% endblock %}
